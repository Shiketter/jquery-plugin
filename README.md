#jquery-float-area.js
You can use this plug-in, you will be able to move as you move the mouse the range that you specify.

use
```javascript:jquery-float-area.js
$(function() {
    $(element).floatArea();
});
```

default
* top: 100px
* duration : 900

set options
```javascript:jquery-float-area.js
$(function() {
    $(element).floatArea({top:30, duration:500});
});
```
